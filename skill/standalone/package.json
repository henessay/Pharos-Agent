{
  "name": "tx-guard",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone tx-guard skill package — the risk-engine core (incl. viem) is bundled into lib/guard-skill.mjs, so the scripts run with Node >= 20 alone: no install, no build step.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  }
}
